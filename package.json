{
  "name": "protui",
  "private": true,
  "description": "Tooling-only manifest: prettier formats the non-Go files in this repository. protui itself is a Go program and needs no Node.js to build or run.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.8"
  }
}
